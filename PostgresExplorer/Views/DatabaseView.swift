import SwiftUI

struct DatabaseView: View {
    var body: some View {
        // всякие навигатион сплит вью и прочее но потом
    }
}

#Preview {
    DatabaseView()
        .frame(width: 800, height: 500)
}

